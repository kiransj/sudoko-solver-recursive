This program solves a sudoku and is implemented in C. If the particular 
sudoko game needs to guess a value then this algoritm guesses it by 
trying out all combination using recursive solution. 

To compile type
make

to use the program run

./a.out <filename>

filename is the name of the file which has the sudoko to be solved.
See the sample file with the repo
